[package]
name = "capture-screen-encode"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
publish = false
description = "Windows screen H.264 encoder over Media Foundation (IMFSinkWriter); an inert stub on non-Windows hosts."

[lints]
workspace = true

[dependencies]
observer-model.workspace = true
observer-nv12.workspace = true
observer-sample-timing.workspace = true
observer-segment.workspace = true

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Media_MediaFoundation",
    "Win32_System_Com",
] }
