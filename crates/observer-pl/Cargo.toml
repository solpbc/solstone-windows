[package]
name = "observer-pl"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
publish = false
description = "Pure observer PL wire protocol: protocol-v3 ingest and reconcile wire types, multipart envelope, custody proofs, and civil date math. No platform dep, no I/O; host-testable."

[lints]
workspace = true

[dependencies]
observer-model.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
base64.workspace = true
spl-core.workspace = true
time = { version = "0.3.54", features = ["parsing"] }
xtask = { path = "../../xtask" }
