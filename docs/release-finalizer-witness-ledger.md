# Release-finalizer witness ledger

This ledger is an evidence index. A row records only the boundary exercised by
its pointer; it does not turn fake-tool, parser, or host orchestration coverage
into native Windows or cryptographic proof. Box-only result cells remain
placeholders until an operator records the real run.

The canonical source-bound transaction produces the current-only
`target/release-candidate/<VERSION>/` six/seven-artifact bundle plus companion
manifest (seven/eight files total), distinct from the accumulated internal
`Releases/` workspace. Receipts remain outside it under
`target/release-evidence/<VERSION>/`. Every direct publication entry point stays
fail-closed; publication belongs only to the aggregate provenance publisher.

| ID | Claim | Status | Evidence pointer | Limits | Post-ship result/date |
|---|---|---|---|---|---|
| AC1-host | The finalizer binds a full local commit, allowed ref, clean tree, and both lock digests, then rejects late drift without remote lookup. | `witnessed-by-ci-with-fakes` | `release_source_binding::source_binding_accepts_main_and_sync_and_uses_only_local_git_operations`; `release_finalizer::source_binding_mutations_abort_phase_one_without_mutation_or_build`; `release_finalizer::late_source_lock_and_post_hash_mutations_block_promotion` | Uses fake git output and host files; it does not prove a transferred Windows checkout. | n/a |
| AC1-transfer-host | The transfer driver carries and compares exactly one commit, Cargo lock digest, and UI lock digest and refuses malformed acknowledgements without a later remote call. | `witnessed-by-ci-with-fakes` | `scripts/lib/deterministic-gates.test.sh` three-field binding cases; `make test-scripts` | Uses fake ssh/scp and a host git fixture; it does not execute the box command processor. | n/a |
| AC1-box | The real box rejects a transferred wrong HEAD or either wrong lock digest before byte-changing work. | `witnessed-only-post-ship-on-the-box` | Operator: `WIN_REMOTE_HOST=<box> EXPECTED_RELEASE_COMMIT=<commit> make win-host-ci`, including deliberate HEAD/Cargo/UI mismatch runs | Not earned by shell fakes; requires the actual transferred bundle, box checkout, and `scripts/win-ci.cmd`. | PENDING - result/date: ____ |
| AC2 | Cleanup is limited to the explicit catalog and refuses links, reparse points, collisions, special files, unknown entries, or changed identity before deleting anything. | `witnessed-by-ci-with-fakes` | `release_finalizer_fs::{catalog_materializes_only_the_enumerated_version_namespaces,symlink_at_every_catalog_leaf_refuses_before_deletion,symlink_at_every_catalog_root_refuses_before_deletion,symlink_at_every_intermediate_ancestor_refuses_before_deletion}`; `release_finalizer::cleanup_confinement_mutations_refuse_whole_cleanup_and_preserve_outside_bytes` | Linux symlinks and a reparse seam do not exercise a live Windows junction. | n/a |
| AC3 | Finalization earns advisory time only after a pinned-key minisign-verified freshness packet, exact signed-commit/HEAD binding, an isolated full private-mirror snapshot, and a successful selected offline cargo-deny action. | `witnessed-by-ci-with-fakes` | `release_advisories::{deployed_freshness_protocol_rendering_is_byte_exact,mirror_key_pin_and_minisign_fail_before_repository_inspection,freshness_and_commit_boundaries_are_exact,clean_full_fresh_snapshot_passes_with_public_provenance,checked_at_is_earned_only_after_cargo_deny_success}`; `release_finalizer::advisory_snapshot_mutations_never_earn_checked_at_or_start_a_build` | Minisign, snapshot Git, and cargo-deny execution are faked here; this row does not prove a current production packet. | n/a |
| AC3-real-pin | The generated advisory config is accepted offline by the real pinned cargo-deny 0.20.2. | `witnessed-by-ci-with-fakes` | `make check-release-advisory-config` | Despite the ledger's two-value status vocabulary, this AC3 row invokes the real host cargo-deny binary, not a fake; it is not Windows evidence and not the release-only 24-hour freshness proof. | n/a |
| AC4-host | Make and direct PowerShell entry points delegate through tool preflight, version, lock, npm-cache preflight, and exactly one finalizer invocation before native actions; the root ignore policy hides only Tauri capability schemas in the generated tree. | `witnessed-by-ci-with-fakes` | `scripts/lib/make-package-ordering.test.sh`; `scripts/lib/package-entrypoints.test.ps1`; `scripts/lib/deterministic-gates.test.sh` real-git Tauri ignore case | Make and real-git status are executable host evidence; PowerShell fakes do not prove the real `.cmd` wrapper or that npm's offline dry run distinguishes warm from incomplete caches on Windows. | n/a |
| AC4-cmd-box | The actual `.cmd` wrappers execute under Windows cmd/PowerShell and reach exactly one source-bound finalizer. | `witnessed-only-post-ship-on-the-box` | Operator: run `scripts/win-package.cmd` and the `.cmd` path exercised by `scripts/win-ci.cmd` with witness logging | Host source inspection cannot prove Windows quoting, codepage, or command-processor behavior. | PENDING - result/date: ____ |
| AC5 | Finalization assembles a newly empty sibling candidate and atomically promotes the complete directory without piecemeal final writes. | `witnessed-by-ci-with-fakes` | `release_finalizer_fs::{candidate_temp_is_new_empty_and_promotes_atomically,candidate_promotion_refuses_any_existing_final_target}`; finalizer happy-path tests | Host filesystem rename behavior does not prove the Windows filesystem implementation on the box. | n/a |
| AC6 | The current bundle reconciles setup naming and contains exactly six artifacts, or seven with a ledger-agreed current delta, plus the companion. | `witnessed-by-ci-with-fakes` | `release_finalizer::{unsigned_happy_path_promotes_exact_bundle_without_signer,explicit_delta_base_promotes_the_exact_eight_file_bundle,velopack_inventory_assets_and_ledger_mutations_never_reach_candidate_evidence}` | Velopack itself is a fake emitter; exact real vpk 1.2.0 output remains part of the signed box run. | n/a |
| AC7 | The producer uses the shared typed evidence model, canonical side-effect-free renderer, filename authority, and strict classifier. | `witnessed-by-ci-with-fakes` | `rust_release_manifest::{rust_release_manifest_good_modes_accept_and_report_boundaries,rust_release_manifest_renderer_is_cross_root_deterministic}`; finalizer happy-path tests | Proves bytes and classification on inert artifacts, not native artifact provenance. | n/a |
| AC8-host | Signed-mode policy parsing and SignTool grammar are fail-closed and selected-path-bound; the finalizer constructs both signing child environments from the pinned selection record, and MSVC/vcvars activation is not what supplies SignTool. Unsigned mode invokes no signer. | `witnessed-by-ci-with-fakes` | `release_selection::{signing_child_environment_prepends_selected_signtool_directory,signing_child_environment_requires_selected_signtool_and_usable_parent}`; `release_signing::{accepted_verbose_grammar_uses_selected_tool_and_yields_signed_verified,missing_duplicate_unsigned_and_wrong_leaf_are_rejected,chain_timestamp_exit_and_grammar_failures_are_distinct,unsigned_mode_provably_invokes_no_signer}`; `release_finalizer::signed_happy_path_keeps_stage_unsigned_and_uses_signed_container_baseline` | Fake SignTool text is parser/action evidence only; it is not a real certificate, chain, timestamp, or signature proof. Verify output states no timestamp protocol, so the parser proves a verified timestamp chain rather than an RFC 3161 classification. | n/a |
| AC8-box | KeyLocker signs the real emitted setup and the selected SignTool verifies the approved leaf, trusted chain, and timestamp chain. | `witnessed-only-post-ship-on-the-box` | Operator: set the mirror packet environment from the release runbook, then run `EXPECTED_RELEASE_COMMIT=<commit> SOLSTONE_ADVISORY_TREE_SHA256=<digest> SOLSTONE_SIGN=1 make package`; inspect manifest `native_tools.signing_mode` and finalization receipt signing mode | Requires live KeyLocker credentials, Windows trust policy, real SignTool, and the finalized setup bytes. | PENDING - result/date: ____ |
| AC8a | The full-nupkg and portable canonical executable members share one manifest-bound SHA-256 and byte count; the transaction-bound pre-pack hash is diagnostic only because signed vpk operates on private copies. | `witnessed-by-ci-with-fakes` | `release_container_baseline::{measured_velopack_shapes_produce_the_expected_evidence,comparator_rejects_hash_or_size_divergence_between_containers}`; `release_finalizer::{signed_happy_path_keeps_stage_unsigned_and_uses_signed_container_baseline,archive_and_cross_container_mutations_fail_before_manifest_render}` | Fakes reproduce the measured vpk 1.2.0 entry shapes and private-copy signing placement, but do not execute vpk, produce Authenticode/RFC 3161 bytes, or prove future Velopack versions preserve those shapes. | n/a |
| AC9 | Artifact/manifest mutation and source/lock drift after hashing block candidate promotion; the external receipt is cross-bound to the manifest. | `witnessed-by-ci-with-fakes` | `release_finalizer::late_source_lock_and_post_hash_mutations_block_promotion`; `release_receipt::{finalization_receipt_stages_then_atomically_replaces_existing_final_target,promotion_refuses_a_final_target_created_after_staging}` | Host race injection does not prove power-loss behavior on Windows storage. | n/a |
| AC10 | Fixed inputs and fixed time produce byte-identical manifests across distinct roots, temp names, and enumeration order. | `witnessed-by-ci-with-fakes` | `release_finalizer::fixed_inputs_are_deterministic_across_roots_temp_names_and_iteration_order`; `rust_release_manifest::rust_release_manifest_renderer_is_cross_root_deterministic` | Determinism uses inert fake tool outputs; real tool reproducibility is a separate release concern. | n/a |
| AC11 | The full finalizer mutation matrix fails closed with no publishable candidate or green receipt and phase-correct witnesses; ordinary selected-action failures expose a normalized one-line final 512-byte child-output tail, while SignTool grammar-drift failures expose only a typed grammar stage. The finalizer constructs signing-mode child environments from the pinned selection record; MSVC/vcvars activation is not what supplies SignTool. | `witnessed-by-ci-with-fakes` | Engine mutation tests in `xtask/tests/release_finalizer.rs`, especially `phase_one_through_three_gates_precede_all_byte_changing_actions`, `stale_build_output_and_missing_offline_npm_cache_cannot_be_reused`, `signing_grammar_drift_promotes_only_the_typed_certificate_free_stage`, source, cleanup, Velopack, advisory, signing, and post-hash groups; `release_signing::errors_and_results_do_not_leak_private_paths_credentials_or_certificates` | SignTool verification errors retain only typed determinations and parser stages; raw stdout/stderr is never retained because even grammar-drift output can contain certificate material. | n/a |
| AC12 | Repository documentation rejects guidance that makes the optional, non-authoritative GitHub mirror authoritative or required, plus hand-chained build/package/publication guidance. | `witnessed-by-ci-with-fakes` | `scripts/lib/doc-stale-scan.test.sh`; `scripts/lib/doc-stale-scan.sh`; `make test-scripts` | Semantic patterns are intentionally bounded and do not replace human review of novel wording. | n/a |
| AC13 | Every selected release action uses the closed selection record, fixed argv template, absolute program agreement, and an ordered fake witness; child-process checkout paths are UTF-8, allowlist-de-verbatimed at the boundary, and the witness records Cargo target environment plus VPK path argv. | `witnessed-by-ci-with-fakes` | `artifact_fs::{child_process_paths_simplify_only_allowlisted_verbatim_shapes,windows_verbatim_root_supports_containment_and_stable_read}`; `release_selection::{committed_action_templates_match_the_closed_selection_parser,action_path_argv_and_placeholder_drift_are_distinct,sanitized_projection_contains_no_paths_argv_or_environment_values}`; `release_finalizer::unsigned_happy_path_promotes_exact_bundle_without_signer`; finalizer action-order tests | Absolute fake paths do not prove native executable identity; ordinary de-verbatimed paths reintroduce child-tool path-length limits, with no fallback. | n/a |
| AC14 | This ledger separates CI/fake evidence from box-only native and cryptographic evidence with pointers and explicit limits. | `witnessed-by-ci-with-fakes` | `docs/release-finalizer-witness-ledger.md`; doc stale scan | The index is not execution evidence; box rows remain pending until an operator records them. | n/a |
| AC15 | Native proof's atomic wrapper builds and invokes the selected xtask directly, then native proof classifies the whole candidate first, binds its manifest and finalization receipt, accepts only signed-verified tools, reports invalid-UTF-8 resolver stdout distinctly, and rechecks both containers. | `witnessed-by-ci-with-fakes` | `scripts/lib/make-prove-native-ordering.test.sh`; `native_release_proof::signed_candidate_installs_smokes_and_writes_atomic_private_clean_proof`; `native_release_proof_mutations::{invalid_candidate_and_receipt_binding_fail_before_native_actions,resolver_stdout_encoding_fails_before_native_actions,every_executable_identity_source_is_bound_to_the_manifest}` | Candidate, resolver, legacy-codepage bytes, and containers are inert host fixtures; this does not prove the real preflight emits UTF-8 on Windows. | n/a |
| AC16 | Native proof uses only the candidate setup and a newly empty isolated root, requires absent-before/created-after, and rejects successful no-op installation. | `witnessed-by-ci-with-fakes` | `native_release_proof_mutations::{setup_source_is_exactly_the_candidate_canonical_setup,isolated_install_root_and_installer_fail_closed}` | Fake installer behavior does not prove Windows installer semantics. | n/a |
| AC17 | Installed bytes equal the manifest/container baseline, report the exact version through direct STEP_8, and pass explicit-binary smoke with fallback disabled, literal `SMOKE_OK`, and an exact post-launch `/healthz` version. | `witnessed-by-ci-with-fakes` | `scripts/lib/smoke-version-gate.test.ps1`; `native_release_proof_mutations::{every_executable_identity_source_is_bound_to_the_manifest,installed_dump_state_version_is_exact_and_canonical,smoke_selection_and_green_evidence_are_fail_closed}` | Fake dump-state and smoke output plus the isolated health-body gate do not prove a Session-1 health/render result. | n/a |
| AC18 | Post-smoke candidate mutation and every proof receipt/time mutation fail closed with no green receipt and a byte-identical candidate postcondition. | `witnessed-by-ci-with-fakes` | Every case in `xtask/tests/native_release_proof_mutations.rs`, especially `candidate_mutation_during_smoke_invalidates_proof` and `proof_receipt_and_clock_never_report_unearned_success` | Mutation seams do not exercise a live install, UI session, or native receipt. | n/a |
| AC15-18-box | The real preflight emits its selection record as UTF-8 stdout, and a real signed candidate installs into a clean isolated root, runs explicit-binary Session-1 health/render smoke with the exact post-launch `/healthz` version, remains unchanged, and emits a green native-proof receipt. | `witnessed-only-post-ship-on-the-box` | Operator: `make prove-rust-release-native RELEASE_DIR=target/release-candidate/<VERSION>`; inspect `target/release-evidence/<VERSION>/windows-native-proof.json` | Requires the real Windows preflight and pipe bytes, signed setup, interactive session, installed binary, selected native tools, and receipt bytes. | PENDING - result/date: ____ |
| Gate-WIN-CI | The native Windows CI leg compiles/tests the transferred source and emits the bound three-field acknowledgement. | `witnessed-only-post-ship-on-the-box` | Operator: `WIN_REMOTE_HOST=<box> make win-host-ci`; record the three `WIN_CI_*` lines and `WIN_CI_OK` | No host fake can prove MSVC compilation or execution of `scripts/win-ci.cmd`. | PENDING - result/date: ____ |
| Gate-PUBLISH | Direct R2, GitHub, winget, and scoop publication entry points remain fail-closed; any GitHub mirror is optional and non-authoritative. | `witnessed-by-ci-with-fakes` | `scripts/lib/publication-guard.test.sh`; `make test-scripts` | Proves the local guards invoke no fake transport; it does not publish or test aggregate-publisher credentials. | n/a |
| Gate-TRANSPARENCY-HOST | Canonical entry and pointer bytes, trusted-comment binding, snapshot revalidation, archive staging digest, explicit public subset, conditional-write handling, commit ordering, retry, and head-log rules fail closed under mutation. | `witnessed-by-ci-with-fakes` | `transparency_canonicalization`; `transparency_schemas`; `transparency_builders`; `transparency_staging`; `transparency_transport`; `transparency_publisher`; `scripts/lib/transparency-guard.test.sh` | The object plane and archive channel are directory and process fakes; the real minisign host gate proves only throwaway-key signing and tamper rejection. | n/a |
| Gate-TRANSPARENCY-BUCKET | The provisioned object service enforces create-only conditional PUT semantics, mutable preconditions, byte-identical retry, and conflict rejection on a non-locked probe prefix. | `witnessed-only-post-ship-on-the-box` | Operator: run the approved conditional-PUT probe against the provisioned non-locked probe prefix and record every observed HTTP status | Host fakes cannot prove service-side conditional or retention behavior. | PENDING - result/date: ____ |
| Gate-TRANSPARENCY-MINISIGN-BOX | The native operator lane resolves an accepted minisign 0.11 or 0.12 binary and verifies throwaway signed transparency bytes before first publication. | `witnessed-only-post-ship-on-the-box` | Operator: `make gate-minisign` on the release box | The host gate does not prove the binary selected by the native operator environment. | PENDING - result/date: ____ |
