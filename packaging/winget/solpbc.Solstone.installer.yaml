# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.9.0.schema.json

PackageIdentifier: solpbc.Solstone
PackageVersion: 0.2.0
MinimumOSVersion: 10.0.18362.0
Scope: user
InstallerType: exe
InstallModes:
  - silent
  - silentWithProgress
InstallerSwitches:
  Silent: "--silent"
  SilentWithProgress: "--silent"
UpgradeBehavior: install
# The Settings/About UI is a WebView2 surface; the observer relies on the OS
# Evergreen WebView2 runtime (not bundled). Declaring it lets winget install the
# runtime first on the rare machine that lacks it (older Win10/LTSC/Server images).
Dependencies:
  PackageDependencies:
    - PackageIdentifier: Microsoft.EdgeWebView2Runtime
ReleaseDate: 2026-06-23
AppsAndFeaturesEntries:
  - DisplayName: sol
    Publisher: sol pbc
    DisplayVersion: 0.2.0
    InstallerType: exe
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/solpbc/solstone-windows/releases/download/v0.2.0/Solstone-win-Setup.exe
    InstallerSha256: 3D5C427FF802C34731908EE408FDE0ADC84E572ED3513E748B7CA32B36996DAF
ManifestType: installer
ManifestVersion: 1.9.0
