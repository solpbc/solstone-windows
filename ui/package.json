{
  "name": "solstone-windows-ui",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "WebView2 front-end for the solstone Windows observer. Pure renderer: it subscribes to health://changed and renders the honest state; it never mints status.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
